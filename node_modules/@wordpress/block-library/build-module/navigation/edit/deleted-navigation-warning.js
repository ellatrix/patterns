import { createElement } from "@wordpress/element";

/**
 * WordPress dependencies
 */
import { Warning } from '@wordpress/block-editor';
import { Button } from '@wordpress/components';
import { __ } from '@wordpress/i18n';

function DeletedNavigationWarning(_ref) {
  let {
    onCreateNew
  } = _ref;
  return createElement(Warning, null, __('Navigation menu has been deleted or is unavailable. '), createElement(Button, {
    onClick: onCreateNew,
    variant: "link"
  }, __('Create a new menu?')));
}

export default DeletedNavigationWarning;
//# sourceMappingURL=deleted-navigation-warning.js.map