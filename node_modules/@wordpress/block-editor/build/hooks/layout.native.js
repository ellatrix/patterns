"use strict";

var _hooks = require("@wordpress/hooks");

require("./layout.js");

/**
 * WordPress dependencies
 */

/**
 * Internal dependencies
 */
// This filter is removed because layout styles shouldn't be added
// until layout types are supported in the native version.
(0, _hooks.removeFilter)('editor.BlockListBlock', 'core/editor/layout/with-layout-styles'); // This filter is removed because the layout controls shouldn't be
// enabled until layout types are supported in the native version.

(0, _hooks.removeFilter)('editor.BlockEdit', 'core/editor/layout/with-inspector-controls');
//# sourceMappingURL=layout.native.js.map