"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getActiveFormat = getActiveFormat;

var _getActiveFormats2 = require("./get-active-formats");

/**
 * Internal dependencies
 */

/** @typedef {import('./create').RichTextValue} RichTextValue */

/** @typedef {import('./create').RichTextFormat} RichTextFormat */

/**
 * Gets the format object by type at the start of the selection. This can be
 * used to get e.g. the URL of a link format at the current selection, but also
 * to check if a format is active at the selection. Returns undefined if there
 * is no format at the selection.
 *
 * @param {RichTextValue} value      Value to inspect.
 * @param {string}        formatType Format type to look for.
 *
 * @return {RichTextFormat|undefined} Active format object of the specified
 *                                    type, or undefined.
 */
function getActiveFormat(value, formatType) {
  var _getActiveFormats;

  return (_getActiveFormats = (0, _getActiveFormats2.getActiveFormats)(value)) === null || _getActiveFormats === void 0 ? void 0 : _getActiveFormats.find(_ref => {
    let {
      type
    } = _ref;
    return type === formatType;
  });
}
//# sourceMappingURL=get-active-format.js.map