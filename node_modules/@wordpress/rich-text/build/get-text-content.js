"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getTextContent = getTextContent;

var _specialCharacters = require("./special-characters");

/**
 * Internal dependencies
 */

/** @typedef {import('./create').RichTextValue} RichTextValue */
const pattern = new RegExp(`[${_specialCharacters.OBJECT_REPLACEMENT_CHARACTER}${_specialCharacters.LINE_SEPARATOR}]`, 'g');
/**
 * Get the textual content of a Rich Text value. This is similar to
 * `Element.textContent`.
 *
 * @param {RichTextValue} value Value to use.
 *
 * @return {string} The text content.
 */

function getTextContent(_ref) {
  let {
    text
  } = _ref;
  return text.replace(pattern, c => c === _specialCharacters.OBJECT_REPLACEMENT_CHARACTER ? '' : '\n');
}
//# sourceMappingURL=get-text-content.js.map