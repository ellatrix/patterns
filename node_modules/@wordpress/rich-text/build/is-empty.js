"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isEmpty = isEmpty;
exports.isEmptyLine = isEmptyLine;

var _specialCharacters = require("./special-characters");

/**
 * Internal dependencies
 */

/** @typedef {import('./create').RichTextValue} RichTextValue */

/**
 * Check if a Rich Text value is Empty, meaning it contains no text or any
 * objects (such as images).
 *
 * @param {RichTextValue} value Value to use.
 *
 * @return {boolean} True if the value is empty, false if not.
 */
function isEmpty(_ref) {
  let {
    text
  } = _ref;
  return text.length === 0;
}
/**
 * Check if the current collapsed selection is on an empty line in case of a
 * multiline value.
 *
 * @param {RichTextValue} value Value te check.
 *
 * @return {boolean} True if the line is empty, false if not.
 */


function isEmptyLine(_ref2) {
  let {
    text,
    start,
    end
  } = _ref2;

  if (start !== end) {
    return false;
  }

  if (text.length === 0) {
    return true;
  }

  if (start === 0 && text.slice(0, 1) === _specialCharacters.LINE_SEPARATOR) {
    return true;
  }

  if (start === text.length && text.slice(-1) === _specialCharacters.LINE_SEPARATOR) {
    return true;
  }

  return text.slice(start - 1, end + 1) === `${_specialCharacters.LINE_SEPARATOR}${_specialCharacters.LINE_SEPARATOR}`;
}
//# sourceMappingURL=is-empty.js.map